DROP TABLE IF EXISTS `$project$.$dataset$.ST2`;
CREATE TABLE `$project$.$dataset$.ST2`
(
    `ADDRESS_ID2222` STRING,
     `INDIVIDUAL_ID` STRING,
      `FIRST_NAME` STRING,
       `LAST_NAME` STRING
);
