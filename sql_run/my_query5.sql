DROP TABLE IF EXISTS `$project$.$dataset$.ST5`;
CREATE TABLE `$project$.$dataset$.ST5`
(
    `ADDRESS_ID5555` STRING,
     `INDIVIDUAL_ID` STRING,
      `FIRST_NAME` STRING,
       `LAST_NAME` STRING
);
